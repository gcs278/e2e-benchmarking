apiVersion: gateway.networking.k8s.io/v1alpha2
kind: HTTPRoute
metadata:
  name: http-perf-reencrypt-{{.Replica}}
spec:
  parentRefs:
  - name: gateway-perf
    namespace: gwapi
  hostnames: ["{{.Replica}}.http-perf-reencrypt.gwapi.bm.example.com"]
  rules:
  - backendRefs:
    - name: http-perf-{{.Replica}}
      port: 8443
